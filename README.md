# Delivery App in web style
## manage users admin and client
### To install
change .env -u user -p password to connect database SQL
composer install
composer database
composer migrations

in dev composer serve
